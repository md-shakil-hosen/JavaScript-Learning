// Find Largest Number in array 

const numbers = [52, 45, 45, 52, 12];
function findLargestNumber (numbers){
    const max = numbers[0];
    for(let i = 0; i < numbers.length; i++){
        const elements = numbers[i];
        if(elements < min);
        min = elements;
    }
}

findLargestNumber();